{
  "$id": "https://open-metadata.org/schema/api/createBot.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AddClientCenterToAssetsRequest",
  "description": "Create Request for adding a clientCenter to assets",
  "type": "object",
  "javaType": "org.openmetadata.schema.api.AddClientCenterToAssetsRequest",
  "properties": {
    "operation": {
      "description": "Operation to be performed",
      "type": "string",
      "enum": [
        "AddAssets",
        "AddClientCenterTags"
      ]
    },
    "dryRun": {
      "description": "If true, the request will be validated but no changes will be made",
      "type": "boolean",
      "default": true
    },
    "clientCenterTags": {
      "description": "ClientCenter Tags to be added",
      "type": "array",
      "items": {
        "$ref": "../type/tagLabel.json"
      },
      "default": null
    },
    "assets": {
      "description": "List of assets to be created against which the clientCenter needs to be added.",
      "$ref": "../type/entityReferenceList.json"
    }
  },
  "additionalProperties": false
}
