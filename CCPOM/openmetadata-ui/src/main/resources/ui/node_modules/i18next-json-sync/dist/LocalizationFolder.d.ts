import { FileMap, LineEndings } from './';
export default class LocalizationFolder {
    private hashes;
    private files;
    private primaryLanguage;
    private isReportMode;
    constructor(files: FileMap, primaryLanguage: string, isReportMode: boolean);
    populateFromDisk(filesToCreate: string[]): void;
    private registerMissingFiles;
    flushToDisk(jsonSpacing: string | number, lineEnding: LineEndings, addFinalNewline: boolean): string[];
    getSourceObject(): Object;
    getTargetObject(name: string): object;
    getFilenames(): string[];
}
