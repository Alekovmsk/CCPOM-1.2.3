export default class ActionRecorder {
    private errors;
    private addedKeys;
    private removedKeys;
    private fileName;
    private isReportMode;
    constructor(fileName: string, isReportMode: boolean);
    keyAdded(key: string): void;
    keyRemoved(key: string): void;
    error(message: (fileName: string) => string): void;
    hasAnyActions(): boolean;
    flushToConsole(): void;
    private getErrors;
    hasAnyErrors(): boolean;
    private getMessageForAddedKeys;
    private getMessageForRemovedKeys;
}
